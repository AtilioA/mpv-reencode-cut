{
    "name": "mpv-reencode-cut",
    "version": "1.4.0",
    "description": "MPV script for cutting and re-encoding videos",
    "main": "scripts/mpv-reencode-cut/dist/worker.js",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w --preserveWatchOutput",
        "clean": "rimraf scripts/mpv-reencode-cut/dist",
        "prebuild": "npm run clean",
        "dev": "tsc -w --preserveWatchOutput",
        "check": "tsc --noEmit",
        "test": "npm run build && node --test \"scripts/mpv-reencode-cut/dist/**/*.test.js\"",
        "verify": "npm run check && npm test",
        "predeploy:win": "if exist \"%APPDATA%\\mpv\\scripts\\mpv-reencode-cut\" rmdir /S /Q \"%APPDATA%\\mpv\\scripts\\mpv-reencode-cut\"",
        "deploy:win": "node scripts/package-release.js \"%APPDATA%\\mpv\"",
        "dev:deploy:win": "node scripts/package-release.js \"%APPDATA%\\mpv\"",
        "dev:install:win": "npm run build && node scripts/package-release.js --clean \"%APPDATA%\\mpv\"",
        "dev:install:unix": "npm run build && node scripts/package-release.js --clean \"$HOME/.config/mpv\"",
        "deploy:unix": "node scripts/package-release.js \"$HOME/.config/mpv\"",
        "deploy": "npm run prebuild && npm run build && npm run predeploy:win && npm run deploy:win",
        "package": "node scripts/package-release.js"
    },
    "author": "",
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^20.0.0",
        "archiver": "^6.0.1",
        "rimraf": "^5.0.0",
        "typescript": "^5.0.0"
    }
}
